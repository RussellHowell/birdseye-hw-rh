import * as React from 'react';

interface ILoginLandingProps {
}

const LoginLanding: React.FunctionComponent<ILoginLandingProps> = (props) => {
  return(
      <div>
          Login
      </div>
  );
};

export default LoginLanding;